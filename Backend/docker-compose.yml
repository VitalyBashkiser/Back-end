version: '3'

services:
  web:
    build: .
    command: python manage.py runserver 127.0.0.1:8000
    volumes:
      - ./app/:/usr/src/app/
    ports:
      - 8000:8000
    env_file:
      - .env.dev

  db:
    image: postgres
    environment:
      POSTGRES_DB: BASE
      POSTGRES_USER: Vitalii
      POSTGRES_PASSWORD: 34500_R

  redis:
    image: redis

    volumes:
      - .:/code